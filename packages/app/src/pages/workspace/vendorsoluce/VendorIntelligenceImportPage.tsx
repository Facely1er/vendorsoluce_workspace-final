import React from 'react';
import { CheckCircle2, DatabaseZap, UploadCloud } from 'lucide-react';
import { useAuth } from '../../../context/AuthContext';
import { importVendorGraphCsv } from '../../../services/vendorsoluce';
import WorkspacePageShell from '../../../components/vendorsoluce-intelligence/WorkspacePageShell';
import PanelCard from '../../../components/vendorsoluce-intelligence/PanelCard';
import { Button } from '../../../components/ui/Button';
import { resolveOrgId } from './utils';
const SAMPLE_PLACEHOLDER = 'Paste CSV content here…';
const VendorIntelligenceImportPage: React.FC = () => {
  const { user } = useAuth();
  const orgId = React.useMemo(() => resolveOrgId(user), [user]);
  const [vendorsCsv, setVendorsCsv] = React.useState('');
  const [servicesCsv, setServicesCsv] = React.useState('');
  const [processingCsv, setProcessingCsv] = React.useState('');
  const [edgesCsv, setEdgesCsv] = React.useState('');
  const [status, setStatus] = React.useState<string | null>(null);
  const [busy, setBusy] = React.useState(false);
  const [error, setError] = React.useState<string | null>(null);
  const canImport = Boolean(orgId && vendorsCsv.trim() && servicesCsv.trim() && processingCsv.trim() && edgesCsv.trim());
  const handleImport = async () => { if (!orgId) { setError('No organization id is available for this session.'); return; } setBusy(true); setError(null); setStatus(null); try { const result = await importVendorGraphCsv(orgId, { vendorsCsv, servicesCsv, processingCsv, edgesCsv }); setStatus(`Imported ${result.insertedNodes} nodes and ${result.insertedEdges} edges, then recomputed metrics.`); } catch (e) { setError(e instanceof Error ? e.message : 'Import failed'); } finally { setBusy(false); } };
  return <WorkspacePageShell eyebrow="VendorSoluce dependency intelligence" title="Dependency graph import" description="Import the canonical four-file dependency bundle — vendors, services, processing, and edges — then recompute graph metrics in one controlled operation." actions={[{ label: busy ? 'Importing…' : 'Run import', onClick: handleImport, variant: 'primary' }]} stats={[{ label: 'Input set', value: '4 CSV files', hint: 'Required for graph build' }, { label: 'Recompute', value: 'Automatic', hint: 'Metrics rerun after import' }]}>{error ? <div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700 dark:border-red-900/70 dark:bg-red-950/30 dark:text-red-300">{error}</div> : null}{status ? <div className="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700 dark:border-emerald-900/70 dark:bg-emerald-950/30 dark:text-emerald-300">{status}</div> : null}<div className="grid gap-6 xl:grid-cols-[1.4fr,1fr]"><div className="grid gap-6">{[['vendors.csv', vendorsCsv, setVendorsCsv], ['services.csv', servicesCsv, setServicesCsv], ['processing.csv', processingCsv, setProcessingCsv], ['edges.csv', edgesCsv, setEdgesCsv]].map(([label, value, setter]) => <PanelCard key={label as string} title={label as string} description="Paste the raw CSV file content exactly as generated by your source bundle."><textarea value={value as string} onChange={(e) => (setter as React.Dispatch<React.SetStateAction<string>>)(e.target.value)} placeholder={SAMPLE_PLACEHOLDER} className="min-h-[180px] w-full rounded-xl border border-gray-200 bg-white px-4 py-3 text-sm text-gray-900 outline-none ring-0 transition placeholder:text-gray-400 focus:border-emerald-500 dark:border-gray-800 dark:bg-gray-950 dark:text-white" /></PanelCard>)}</div><div className="grid gap-6"><PanelCard title="Import requirements" description="The graph engine expects a coherent four-file set so the node and edge identities line up."><div className="space-y-4 text-sm text-gray-600 dark:text-gray-300"><div className="flex items-start gap-3"><UploadCloud className="mt-0.5 h-4 w-4 text-emerald-600" /><span>Use the same org id across imports. Mixed tenant ids will fragment the graph.</span></div><div className="flex items-start gap-3"><DatabaseZap className="mt-0.5 h-4 w-4 text-emerald-600" /><span>The import pipeline upserts graph nodes, replaces graph edges, then recomputes vendor metrics.</span></div><div className="flex items-start gap-3"><CheckCircle2 className="mt-0.5 h-4 w-4 text-emerald-600" /><span>After import, review portfolio coverage and vendor linkage before relying on scenario outputs.</span></div></div></PanelCard><PanelCard title="Execution" description="Run the import only when all four CSV datasets are present and scoped to the same portfolio snapshot."><Button variant="primary" onClick={handleImport} disabled={!canImport || busy} className="w-full justify-center">{busy ? 'Importing dependency graph…' : 'Import and recompute'}</Button>{!orgId ? <div className="mt-3 text-sm text-amber-700 dark:text-amber-300">No organization id is available in the current auth context.</div> : null}</PanelCard></div></div></WorkspacePageShell>;
};
export default VendorIntelligenceImportPage;
