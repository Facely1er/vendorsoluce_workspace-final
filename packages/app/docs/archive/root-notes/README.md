# Archived app root notes

These files were moved out of the `packages/app` root during normalization to reduce noise and make the package look like a production codebase instead of a running logbook.

They are kept for traceability only.
